import { CapitalizestrwordsPipe } from './capitalizestrwords.pipe';

describe('CapitalizestrwordsPipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalizestrwordsPipe();
    expect(pipe).toBeTruthy();
  });
});
